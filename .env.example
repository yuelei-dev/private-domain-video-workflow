PORT=3000

# 远程素材 API 应返回 {"items": [...]}，每项可包含 title/name 与 video_url/url/file。
MATERIAL_API_URL=
MATERIAL_FILE_BASE_URL=
MATERIAL_BEARER_TOKEN=

# BGM 清单应返回 {"tracks": [...]}，每项可包含 title 与 url，或 file + base。
BGM_MANIFEST_URL=
BGM_FILE_BASE_URL=
BGM_BEARER_TOKEN=
